import {Text, View} from "react-native";
const Home = () => {
    return (
        <View className="flex items-center justify-center">
            <Text>index</Text>
        </View>
    );
};

export default Home;
